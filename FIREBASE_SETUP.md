# Firebase Setup Instructions

This project now uses Firebase Authentication with PingOne OpenID Connect.

Use the complete setup guide in `FIREBASE_OIDC_SETUP.md`.

Legacy Anonymous Auth instructions are intentionally removed to avoid insecure write access.
